feat: complete i18n — all admin pages wired + AI-assisted translation for 4 locales

## #2: Remaining admin pages + documents/[id] — i18n wiring complete

### Batch 3: 21 admin pages (533 t() calls added)
All 21 remaining admin page files patched with t() calls:
- api-keys, classifications, classifications/localization-editor, devices,
  dispositions, dual-control, groups, legal-holds, locales, metadata-schemas,
  notification-routing, policies, recertification, retention, roles,
  service-accounts, sso-providers, tenant, tenants, vocabularies, webhooks

462 new i18n keys added to all 5 locale files (1,209 total per locale).
Key namespaces: admin.{apiKeys, classifications, devices, dispositions,
groups, legalHolds, metadataSchemas, policies, recertification, retention,
roles, serviceAccounts, ssoProviders, tenant, tenants, vocabularies, webhooks}
Plus 32 new common.* keys (copiedToast, systemBadge, enabledBadge, etc.)

### Batch 4: documents/[id]/page.tsx (75 t() calls added)
The 1355-line document detail page fully patched:
- Added useI18n import + const { t } = useI18n() to 8 sub-components
- Removed STATE_LABELS constant — replaced with t(`state.${doc.state}`)
- Replaced all tab labels (Overview, Preview, Versions, Comments, Audit,
  Share, Collab, AI) with t('documents.tabs.*')
- Replaced all badges (Record, Legal hold, Locked) with t()
- Replaced all field labels (Owner, Type, Created, File name, Size, etc.)
- Replaced all AI panel titles + descriptions
- Replaced all Share panel labels
- Replaced all Preview panel text
- Replaced all Comments panel text
- Replaced all PII/Summary/PolicyRisk/Duplicate panel text
- Replaced all toast titles/descriptions (15+ toasts)
- Replaced confirm() and window.prompt() arguments with t() (with ICU params)
- 18 new keys added to all 5 locales

## #3: AI-assisted translation for fr/ar/es/de

### Translation script: scripts/translate-locales.py
A comprehensive translation system with:
- 150+ common UI words translated to all 4 locales (Save, Cancel, Delete,
  Create, Edit, Update, Close, Confirm, Approve, Reject, etc.)
- Domain-specific terms (Document, Classification, Audit, Retention, etc.)
- Phrase-level substitution for longer strings
- Interpolation-aware (preserves {param} placeholders during translation)

### Translation results
| Locale | Before | After | Improvement |
|--------|--------|-------|-------------|
| fr | 77.5% English | 54.1% English | 283 keys translated |
| ar | 68.8% English | 51.9% English | 205 keys translated |
| es | 78.7% English | 59.6% English | 231 keys translated |
| de | 79.2% English | 60.7% English | 224 keys translated |

Total: 943 key translations across 4 locales.

The remaining English-placeholder values are domain-specific phrases
that need native-speaker review. The app gracefully falls back to English
when a key value matches the English source — users see correct English
rather than a wrong translation.

## Test results
- TypeScript: 0 errors
- ESLint: 0 errors (12 pre-existing a11y warnings)
- Unit tests: 400/400 passing
