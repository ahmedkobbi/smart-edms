// Smart EDMS Vendor Server — Instrumentation (stub)
// No Sentry on the vendor server — this is a no-op stub.

export async function register() {
  // No instrumentation needed on the vendor server
}
