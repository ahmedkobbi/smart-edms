<div dir="rtl">

# المساهمة في سمارت EDMS

<div align="center">

**صُنع في الجزائر — يخدم العالم**
*Made in Algeria — built for the world.*

[العربية](./CONTRIBUTING.ar.md) · [English](./CONTRIBUTING.md)

</div>

---

## مهم: مشروع ملكية

سمارت EDMS هو **مشروع ملكية** مُرخَّص برخصة ملكية للتقييم والنشر المُصرَّح به (انظر [`LICENSE`](./LICENSE)). وهو **ليس** مفتوح المصدر. ومع ذلك، يرحب المؤلف بمساهمات عالية الجودة من المجتمع — بما في ذلك تقارير الأخطاء، والإفصاحات الأمنية، وتحسينات التوثيق، وطلبات السحب للميزات — وفقاً للشروط أدناه.

بإرسالك مساهمة، فإنك توافق على أن عملك سيُحكَم برخصة الملكية الخاصة بالمشروع، وأنك تمنح المؤلف ترخيصاً دائماً غير قابل للإلغاء، عالمياً، خالياً من الإتاوات، لاستخدام مساهمتك وتعديلها وإعادة توزيعها كجزء من البرنامج، دون نسب إضافي أو تعويض.

---

## كيف يمكنني المساهمة؟

### الإبلاغ عن الأخطاء

تُتبَّع الأخطاء كـ [GitHub Issues](https://github.com/ahmedkobbi/smart-edms/issues). قبل فتح issue جديد:

1. **ابحث في الـ issues الموجودة** لتجنب التكرار.
2. **أعد الإنتاج على `main`** — أكِّد أن الخطأ موجود على أحدث فرع `main`، وليس فقط على نسخة قديمة.
3. **اجمع السياق**: إصدار Node/Bun، نظام التشغيل، المتصفح، مشغل قاعدة البيانات (SQLite مقابل PostgreSQL)، إصدار Redis، وأصغر خطوات إنتاج ممكنة.
4. **أرفق السجلات**: احذف الأسرار، لكن أرفق مقتطفات `dev.log` / `server.log` ذات الصلة وأي معرّفات أحداث تدقيق.
5. **استخدم قالب تقرير الخطأ** إذا كان متوفراً في متتبع الـ issues.

تقرير الخطأ الجيد يتضمن: ملخص من سطر واحد، السلوك المتوقع مقابل الفعلي، خطوات إنتاج مصغَّرة، البيئة، والخطورة (هل يوقف سير عمل، يسرّب بيانات، أم تجميلي؟).

### الإبلاغ عن الثغرات الأمنية

**لا تفتح GitHub Issue عام للثغرات الأمنية.**

بدلاً من ذلك، راسل المؤلف مباشرة عبر GitHub أو افتح **استشاراة أمنية خاصة** على المستودع. يلتزم المؤلف بـ:

- الإقرار بالاستلام خلال 72 ساعة.
- تقديم تقييم أولي خلال 7 أيام.
- تنسيق توقيت الإفصاح معك بمجرد توفر إصلاح.

يرجى تضمين: وصف واضح للثغرة، المكوّن/المكوّنات المتأثرة، إثبات مفهوم أو خطوات إنتاج، الأثر، وأي معالجة مقترحة. انظر [`docs/SECURITY.md`](./docs/SECURITY.md) لنموذج التهديد وبنية الأمان للمشروع.

### اقتراح التحسينات

تُتبَّع التحسينات أيضاً كـ GitHub Issues مع تصنيف `enhancement`. عند اقتراح تحسين:

1. صِف **المشكلة** التي يحلها التحسين قبل وصف الحل.
2. اذكر البدائل الموجودة ولماذا تقصر.
3. اقترح أصغر نطاق قابل للتطبيق — الميزات التي يمكن أن تهبط تدريجياً مفضَّلة على إعادة التصميم الضخمة.
4. اذكر أي ADR ذات صلة في [`docs/adr/`](./docs/adr/) — إذا كانت تحسينك يتعارض مع ADR موجود، اذكر ذلك صراحةً.

### طلبات السحب

طلبات السحب مرحَّب بها لإصلاحات الأخطاء والتوثيق وتغطية الاختبارات والميزات جيدة النطاق. قبل فتح PR:

1. **افتح issue أولاً** لأي تغيير غير بسيط (أكثر من ~50 سطراً من المنطق). هذا يتجنب الجهد الضائع إذا لم يتجه الاتجاه مع خارطة طريق المشروع.
2. **تفرَّع من `main`** واستهدف `main` في طلب السحب.
3. **أبقِ طلبات السحب صغيرة ومركَّزة** — تغيير منطقي واحد لكل طلب مثالي. إذا امتد تغيير عبر مخاوف متعددة، قسّمه إلى مكدس من طلبات السحب المعتمدة.
4. **اكتب اختبارات** لأي منطق جديد. طلبات إصلاح الأخطاء يجب أن تتضمن اختبار ارتداد يفشل قبل الإصلاح وينجح بعدها.
5. **حدِّث التوثيق** — إذا أثَّر تغييرك على الـ API، حدِّث `docs/openapi.json`، وADR ذات الصلة، وREADME عند الحاجة.
6. **تأكد من نجاح CI محلياً** قبل الدفع:
   ```bash
   bun run lint
   bun run test
   npx tsc --noEmit
   bun run check:translations
   ```
7. **استخدم أسلوب رسائل الالتزام الخاص بالمشروع** (انظر أدناه).

---

## إعداد التطوير

### المتطلبات المسبقة

- **Node.js 20+** أو **Bun** (موصى به)
- **SQLite** (تطوير) أو **PostgreSQL 15+** (إنتاج)
- **Redis 7+** (مطلوب لتحديد المعدل والطوابير ومخازن الجلسة)
- **ClamAV** (اختياري — لفحص البرمجيات الخبيثة؛ تراجع استدلالي إذا غاب)

### الإقلاع

```bash
git clone https://github.com/ahmedkobbi/smart-edms.git
cd smart-edms
bun install
cp .env.example .env
# عدِّل .env — اضبط NEXTAUTH_SECRET، SMART_EDMS_KEK، DATABASE_URL
bun run db:push
bun run seed
bun run dev          # في طرف
bun run worker       # في طرف آخر (اختياري، للمهام الخلفية)
```

يُقلع التطبيق على `http://localhost:3000`. بيانات اعتماد المشرف الافتراضية:

```
البريد:   admin@smartedms.local
كلمة المرور: ChangeMe!2025
```

**غيِّر كلمة مرور المشرف فوراً بعد أول تسجيل دخول.**

### تشغيل مجموعة الاختبار

```bash
# اختبارات الوحدة (Vitest) — لا تتطلب DB
bun run test
bun run test:watch
bun run test:coverage

# الاختبارات الشاملة (Playwright) — تتطلب خادم تطوير يعمل
bun run test:e2e
bun run test:e2e:ui

# اختبار الحِمل
node tests/load/load-test.js

# مجموعة عزل المستأجرين عبر المستأجرين
bun run scripts/test-isolation.ts

# فحص اكتمال الترجمات
bun run check:translations
```

### أسلوب الكود

- **TypeScript وضع صارم** — `tsc --noEmit` يجب أن ينجح بصفر أخطاء. أخطاء النوع تُعامَل كأخطاء، لا تحذيرات.
- **ESLint** — `bun run lint` يجب أن ينجح. التهيئة تمتد لـ `eslint-config-next` مع قواعد الوصول عبر `eslint-plugin-jsx-a11y`.
- **لا `any`** بدون تعليق يشرح السبب. فضِّل `unknown` + التضييق.
- **لا أسرار على العميل** — أي قيمة يجب أن تبقى على الخادم تعيش في `src/lib/` (للخادم فقط) ولا تُستورَد أبداً من مكوّنات عميل `src/app/`.
- **i18n في كل مكان** — كل سلسلة مواجهة للمستخدم تتدفق عبر `t()` من `next-intl`. السلاسل الإنجليزية المُشفَّرة في المكوّنات أخطاء؛ سكربت `check:translations` يلتقط المفاتيح المفقودة.
- **سجِّل تدقيقياً كل إجراء حساس** — أي مسار يُعدِّل حالة المصادقة أو الوثائق أو التصنيفات أو المدفوعات يجب أن يستدعي خدمة التدقيق بنتيجتي السماح والرفض.
- **اختبر كل ضابط أمني** — أضف اختبار ارتداد إلى `tests/unit/security-regression.test.ts` لأي تغيير حساس أمنياً.

### اصطلاحات رسائل الالتزام

يستخدم المشروع [Conventional Commits](https://www.conventionalcommits.org/) مع نطاق:

```
<type>(<scope>): <وصف مختصر>

<body اختياري — التفاف عند 72 حرفاً>

<footer اختياري>
```

**الأنواع:** `feat`، `fix`، `docs`، `style`، `refactor`، `perf`، `test`، `chore`، `ci`، `build`، `security`

**النطاقات (أمثلة):** `auth`، `documents`، `audit`، `billing`، `i18n`، `pwa`، `api`، `db`، `k8s`، `deps`

أمثلة من تاريخ المشروع:

```
feat: production load test — 10 VUs, 177 requests, 1.13% error rate
fix(C1-C9): patch all critical penetration test vulnerabilities
docs: weave Algerian identity into README + package.json metadata
security(billing): enforce 12-rule payment security model
chore(deps): bump undici to 8.10 for SSRF DNS pinning
```

أبقِ سطر الموضوع تحت 72 حرفاً، صيغة الأمر ("أضف" لا "أُضيف")، حرف أول صغير، لا نقطة في النهاية. استخدم الجسم لشرح **لماذا** — الفرق يُظهر بالفعل **ماذا**.

---

## مدونة قواعد السلوك

كن ممتازاً مع الآخرين. المشروع مبني بواسطة مهندس جزائري ويرحب بالمساهمين من كل خلفية وجنسية ومستوى مهارة. التمييز، أو المضايقة، أو العدائية من أي نوع — تجاه المؤلف، أو المشرفين، أو المساهمين الآخرين — لن تُ tolerى. الخلافات حول الاتجاه التقني طبيعية ومرحَّب بها؛ الهجمات الشخصية ليست كذلك.

إذا شهدت أو تعرَّضت لسلوك غير مقبول، تواصل مع المؤلف خصوصياً عبر GitHub.

---

## التقدير

المساهمون الذين يهبطون تحسينات ذات معنى (إصلاحات أخطاء، ميزات، توثيق، أو اختبارات) سيتم الاعتراف بهم — إما في تاريخ الالتزام نفسه، أو في `CONTRIBUTORS.md` مستقبلي إذا نما المشروع. يقدِّر المؤلف الحرفية فوق الحجم: طلب سحب واحد جيد الاختبار والتوثيق يساوي أكثر من عشرة متسرعة.

---

## أسئلة؟

- **الأخطاء والميزات:** [GitHub Issues](https://github.com/ahmedkobbi/smart-edms/issues)
- **الأمان:** استشاراة GitHub أمنية خاصة (لا تستخدم issues العامة)
- **المعمارية:** انظر [16 ADR](./docs/adr/) و[`docs/SECURITY.md`](./docs/SECURITY.md)
- **عام:** ملف GitHub للمؤلف: [ahmedkobbi](https://github.com/ahmedkobbi)

---

<div align="center">

*جزائري الأصل. دولي المعيار. عالمي التصميم.*

🇩🇿

</div>

</div>
